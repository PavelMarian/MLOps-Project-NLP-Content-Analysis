services:
  api:
    build: .
    container_name: toxicity-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app:/app/src:/app/src/api
      - MLFLOW_TRACKING_URI=mlruns
      - MLFLOW_ALLOW_FILE_STORE=true
      - PYTHONUNBUFFERED=1
      - HF_HUB_DOWNLOAD_TIMEOUT=300
      - HF_HUB_ETAG_TIMEOUT=60
    volumes:
      - ./toxicity.db:/app/toxicity.db
      - ./mlruns:/app/mlruns
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - monitoring

  prometheus:
    image: prom/prometheus:latest
    container_name: toxicity-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./src/drift_monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    restart: unless-stopped
    networks:
      - monitoring

  grafana:
    image: grafana/grafana:latest
    container_name: toxicity-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./src/drift_monitoring/grafana/grafana-dashboard.json:/etc/grafana/provisioning/dashboards/toxicity-dashboard.json
      - ./src/drift_monitoring/grafana/grafana-datasource.yml:/etc/grafana/provisioning/datasources/datasource.yml
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_SECURITY_ADMIN_USER=admin
    restart: unless-stopped
    networks:
      - monitoring

  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    container_name: toxicity-mlflow
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlruns
    environment:
      - MLFLOW_ALLOW_FILE_STORE=true
    command: mlflow ui --backend-store-uri /mlruns --host 0.0.0.0 --port 5000
    restart: unless-stopped
    networks:
      - monitoring

volumes:
  prometheus_data:
  grafana_data:

networks:
  monitoring:
    driver: bridge